<row>
<div class="col-md-4"></div>
    	<div class="col-md-4"><li class="text-center"><%= gravatar_for user, size: 300 %></li></div>
    	<div class="col-md-4"></div>
</row>
<row>
	<div class="col-md-12 text-center">
     <h2> <%= user.name %></h2>
     <h2> <%= user[:title] %></h2>
     <h3>
     <%= link_to "No", users_path %>
     <%= link_to "Yes", user %>
 </div>
</row>
<row>
	<div class="col-md-12">
  <% if current_user.admin? && !current_user?(user) %>
    <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
	</div>
</row>