<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>

    </aside>
        <div class="span8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center hero-unit">
    <h1>Welcome to Join</h1>

    <h2>
      The dating app for professionals.
    </h2>

    <%= link_to "Sign Dating!", signup_path,
                                class: "btn btn-large btn-primary" %>
  </div>


<% end %>